# API Specification

> **Note:** This document was moved from the repository root.  
> The full original content is in "API Specification.md" at the root (during transition).

Key points:

- Base path: `/api/v1`
- Style: REST + JSON
- Auth: JWT
- Main resources: leads, conversations, messages, follow-ups, activities, health
- FastAPI owns business boundaries; n8n owns workflows
